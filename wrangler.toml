# Cloudflare Pages configuration for Next.js
name = "vortech-website"
compatibility_date = "2026-04-15"

# Build configuration
[build]
command = "npm run build"

# Deploy static assets from .next directory
[assets]
directory = ".next/standalone"
